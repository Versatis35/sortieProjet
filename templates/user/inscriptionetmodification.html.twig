{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}
    {{ form_start(formUser) }} {# <form ...> #}
    <button class="btn btn-success" id="btnAddTeam">
        <i class="fa fa-plus"></i>
    </button>
    {{ form_end(formUser) }} {# <form ...> #}
{% endblock %}